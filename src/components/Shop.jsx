import React from 'react';

const Shop = (props) => {
    return (
        <div className='shop'>
            <img className='shopImg' src={props.img} />
            
        </div>
    );
};

export default Shop;